#include <emulation.h>

int main () {
    // using namespace Components;

    // auto core = new EmulationCore;
    
    // auto satellite = core->NewEmulatedBlock();
    // satellite->AddComponent(Satellite{
    //     Satellite::Config{},
    // });

    // auto satellite = core->NewEmulatedBlock();
    // satellite->AddComponent(Satellite{
    //     Satellite::Config{},
    // });
    
    // auto line1 = EmulationLine(core, satellite, );
    // auto line2 = EmulationLine(core, satellite, 1000);

    // line1.Emulate();

    return 0;
}
